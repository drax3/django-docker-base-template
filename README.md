# 🧩 Django + Docker + Pipenv Base Template

A ready-to-use Django backend setup with Docker, Pipenv, and REST framework preconfigured.

## Features
- Dockerized backend with Pipenv environment
- Custom user model (admin, manager, user)
- JWT authentication (SimpleJWT)
- Ready for API development

## Quick Start
1. Clone this repo (or click "Use this template")
2. Run: `docker-compose up --build`
3. Create Django project/app inside `/backend/code/`
4. Enjoy isolated development with live reload
